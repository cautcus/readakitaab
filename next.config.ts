import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com','assets.aceternity.com','img1.wsimg.com', 'upload.wikimedia.org'],
  },
};

export default nextConfig;
